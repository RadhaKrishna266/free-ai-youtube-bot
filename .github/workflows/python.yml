name: AI Temple Video Bot

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install system tools
        run: |
          sudo apt update
          sudo apt install -y \
            ffmpeg \
            imagemagick \
            wget \
            sox \
            libsox-fmt-all

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        env:
          COQUI_TOS_AGREED: "1"
        run: |
          python -m pip install --upgrade pip
          pip install \
            torch==2.1.0 \
            transformers==4.36.2 \
            google-auth \
            google-auth-oauthlib \
            google-api-python-client \
            requests \
            pillow \
            TTS \
            huggingface_hub

      - name: Download speaker voice
        run: |
          mkdir -p speaker
          wget -O speaker/speaker.wav https://huggingface.co/coqui/XTTS-v2/resolve/main/samples/en_sample.wav

      - name: Pre-download Coqui XTTS v2 model
        env:
          COQUI_TOS_AGREED: "1"
        run: |
          python - << 'EOF'
          from TTS.api import TTS
          TTS("tts_models/multilingual/multi-dataset/xtts_v2", gpu=False)
          EOF

      - name: Run Temple Video Bot
        env:
          PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
          YOUTUBE_TOKEN_BASE64: ${{ secrets.YOUTUBE_TOKEN_BASE64 }}
          COQUI_TOS_AGREED: "1"
        run: |
          python upload.py

      - name: Upload final video
        uses: actions/upload-artifact@v4
        with:
          name: final-temple-video
          path: final.mp4